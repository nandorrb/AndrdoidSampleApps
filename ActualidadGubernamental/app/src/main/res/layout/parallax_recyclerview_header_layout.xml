<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/parallax_recyccer_view_id"
    android:orientation="vertical">

    <ImageView
        android:id="@+id/str_sticky_headr_img"
        android:layout_width="match_parent"
        android:layout_height="180dp"
        android:adjustViewBounds="true"
        android:scaleType="centerCrop"
        android:src="@drawable/ic_instituto_pacifico_libro_dinamic_24dp" />

    <TextView
        android:id="@+id/str_sticky_headr_tv"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentLeft="true"
        android:text="JR"
        android:textColor="@color/defaut_background_color"
        android:textSize="30sp" />
</LinearLayout>